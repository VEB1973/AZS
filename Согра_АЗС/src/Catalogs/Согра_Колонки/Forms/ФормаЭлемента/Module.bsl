
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	локНаименование = "ТРК "+Объект.НомерКолонки+" ("+Объект.ВидТоплива+")";
	Если Объект.Наименование<>локНаименование Тогда
		Объект.Наименование = локНаименование;
	КонецЕсли;	
КонецПроцедуры
